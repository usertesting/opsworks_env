# This file is generated by the opsworks_env cookbook. Update it by setting the
# stack's custom JSON and executing the opsworks_env::default recipe.

<% @env.sort.each do |key, val| %>
export <%= key.to_s %>=<%= (val && val.to_s) %>
<% end %>